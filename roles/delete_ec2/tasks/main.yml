- name: Delete specific EC2 instances
  amazon.aws.ec2_instance:
    state: absent
    name: my-ansible-instance
    instance_ids:
      - "{{ instance_id }}"
    region: us-east-1
  delegate_to: localhost
